//! CLI minimale (docs/015-roadmap.md Phase 2) — valide que le workspace
//! compile et que les crates du Core s'assemblent correctement, avant tout
//! developpement metier reel.

fn main() {
    println!("syfi-cli squelette — workspace pret (docs/004-workspace.md)");
}
