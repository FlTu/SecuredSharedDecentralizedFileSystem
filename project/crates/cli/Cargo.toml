[package]
name = "cli"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "CLI minimale pour valider le squelette sans interface graphique (docs/015-roadmap.md Phase 2)"

[[bin]]
name = "syfi"
path = "src/main.rs"

[dependencies]
common = { path = "../common" }
storage = { path = "../storage" }
vault = { path = "../vault" }
